[package]
name = "game_shared"
edition.workspace = true
version.workspace = true

[lib]
path = "src/lib.rs"

[features]
default = []
diagnostics = []

[dependencies]
compactly.workspace = true
thunder = { path = "../../thunder", default-features = false }
